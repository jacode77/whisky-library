<section class="container d-flex flex-column flex-md-row px-2 my-2 my-md-5">
    <div class="content col-12 col-md-7 col-lg-8 p-0 pr-md-3">
        <div class="col-12 p-0 main-preview mb-4">
            <%=image_tag "placeholder.jpg", alt: "Whisky", class: "thumbnail card-img top" %>
        </div>
    </div>
    <div class="sidebar col-lg-4 p-0 mx-2 d-md-block">
        <h1 class="mb-sm-2 mb-md-5"><%=@listing.title%></h1>
        <h4 class="text-muted">Brand: <%=@listing.brand_id%></h4>
        <%puts `>>>> #{@listing.brand_id} <<<<` %>
        <h4 class="text-muted">Price: <%=format_price(@listing.price)%></h4>
        <h4 class="text-muted">Category: <%=@listing.category_id%></h4>
        <%= puts @listing.category_id %>
        <h4 class="text-muted">Description: <%=@listing.description%></h4>
    </div>
    <%# <div class="container">
    </div> %>
</section>